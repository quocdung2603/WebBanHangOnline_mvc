@model WebBanHangOnline.Models.EF.ProductSize
@{
    ViewBag.Title = "Thêm Size";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutEmpty.cshtml";
}
<h2>Thêm Kích Thước Sản Phẩm</h2>
<!-- Main content -->

<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">@ViewBag.Title</h3>

        </div>
        <div class="card-body">
            @using (Html.BeginForm("Add", "ProductSize", FormMethod.Post))
            {
                @Html.ValidationSummary(true)
                @Html.AntiForgeryToken()
                <div class="form-group">
                    <label>Color</label>
                    @Html.TextBoxFor(x => x.ColorName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.ColorName, null, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <label>Size</label>
                    @Html.TextBoxFor(x => x.SizeName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.SizeName, null, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <label>Số Lượng</label>
                    @Html.TextBoxFor(x => x.Quantity, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.Quantity, null, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-success" id="btnANS">Thêm Mới</button>
                </div>
            }
        </div>
        <!-- /.card-body -->
        <div class="card-footer">

        </div>
        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->
